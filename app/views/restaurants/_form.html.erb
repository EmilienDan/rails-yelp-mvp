<%= simple_form_for(restaurant) do |r| %>
  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :phone_number %>
  <%= r.input :category, collection: %w(chinese italian japanese french belgian) %>
  <%= r.submit %>
<% end %>
